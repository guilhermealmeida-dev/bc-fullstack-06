import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    width: 403,
    height: 225,
    borderRadius: 30,
    // opacity: 0.8,
    backgroundColor:'#69696926',
  },
});
